import { BlogPostDirective } from './blog-post.directive';

describe('BlogPostDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogPostDirective();
    expect(directive).toBeTruthy();
  });
});
