import { ContentfulRichmediaDirective } from './contentful-richmedia.directive';

describe('ContentfulRichmediaDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentfulRichmediaDirective();
    expect(directive).toBeTruthy();
  });
});
